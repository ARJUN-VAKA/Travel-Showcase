{
  "name": "travel-showcase",
  "framework": "vite",
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/db exec drizzle-kit push && pnpm --filter @workspace/trip-portfolio run build && pnpm --filter @workspace/api-server run build && cp -r artifacts/trip-portfolio/dist/public ./public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
