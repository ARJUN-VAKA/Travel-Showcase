{
  "name": "travel-showcase",
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/db exec drizzle-kit push && pnpm --filter @workspace/trip-portfolio run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/trip-portfolio/dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
