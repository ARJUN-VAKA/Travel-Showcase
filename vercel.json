{
  "name": "travel-showcase",
  "framework": "vite",
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/db exec drizzle-kit push && pnpm --filter @workspace/trip-portfolio run build && pnpm --filter @workspace/api-server run build && mkdir -p vercel-dist && cp -R artifacts/trip-portfolio/dist/public/* vercel-dist/",
  "outputDirectory": "vercel-dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
